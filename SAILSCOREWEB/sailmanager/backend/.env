# --- Base de dados / Auth ---
DATABASE_URL=sqlite:///./test.db
SECRET_KEY=troca-este-segredo
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=480

# --- Frontend ---
FRONTEND_BASE_URL=http://localhost:3000

# --- Provisionamento de contas ---
# Opções: temp_password | invite
ACCOUNT_PROVISIONING_MODE=temp_password

# --- SMTP (Mailtrap Sending - transactional / domínio verificado) ---
SMTP_HOST=live.smtp.mailtrap.io
SMTP_PORT=587
SMTP_USER=api
SMTP_PASS=3864d08ab00079b42d0af2c11dce40e9
SMTP_FROM_EMAIL=no-reply@sailscore.online
SMTP_FROM_NAME=SailScore

# --- Branding / replies ---
DEFAULT_CLUB_NAME=SailScore
DEFAULT_CLUB_REPLY_TO=no-reply@sailscore.online
WINDOWS_FORCE_OPEN=true