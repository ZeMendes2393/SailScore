/** Placeholder route — extend when payment verification UI is implemented. */
export default function CheckPaymentsPage() {
  return (
    <div className="p-8">
      <h1 className="text-xl font-semibold text-gray-900">Check payments</h1>
      <p className="text-gray-600 text-sm mt-2">This section is not configured yet.</p>
    </div>
  );
}
